# 📋 Portfolio Roadmap & Tasks (`task.md`)

Список задач и улучшений для усиления конверсии на зарубежных фаундеров, CTO и стартапы (US / EU).

---

## 🚀 Выполнено (Recently Completed)
- [x] **Добавлена тёмная тема (Dark Mode)**
  - [x] Создан `ThemeProvider` (`ThemeContext`) с сохранением выбора в `localStorage` и поддержкой `prefers-color-scheme`.
  - [x] Определена гармоничная палитра CSS-переменных в `app/globals.css` для селектора `[data-theme="dark"]` (глубокий матовый эбеновый тон `#121110`, контрастный текст, акценты и шалфейные заголовки `#92BDB2`).
  - [x] Добавлена компактная кнопка-переключатель (☀️ / 🌙) в `Navbar` рядом с выбором языка.
  - [x] Устранен белый флеш при загрузке (inline script в `<head>`).
  - [x] Заменены жестко закодированные цвета заголовков на адаптивную переменную `var(--header-color)`.

---

## 📌 Бэклог задач (Backlog)

### 1. 🎯 Интеграция реальной ссылки для звонков (`/book`)
- [ ] Заменить плейсхолдер `CALENDLY_URL` в `app/book/page.tsx` на реальную ссылку Calendly или Cal.com.
- [ ] Проверить работу iframe / всплывающего виджета и адаптивность на мобильных устройствах.

### 2. 📊 Усиление кейсов жесткими бизнес- и тех-метриками
- [ ] Добавить в карточки проектов и `ProjectDetailView.tsx` ключевые показатели:
  - Сокращение latency / TTFB (например, *«Latency снижен с 450ms до 65ms»*).
  - Сроки реализации (например, *«Production MVP shipped in 14 days»*).
  - Конкурентная нагрузка и отказоустойчивость (*«10k+ concurrent connections, 99.98% uptime»*).
  - Экономия бюджета / инфра-расходов клиента.

### 3. 🎥 Интерактивные демо и видео (Loom / GIF)
- [ ] Подготовить короткие 10–15 секундные GIF-анимации или видеодемонстрации интерфейсов для 0-to-1 продуктов.
- [ ] Встроить ссылки на Live Demo / GitHub repo в карточки проектов для мгновенного фактчекинга фаундерами.

### 4. 🌐 OpenGraph и Twitter Cards для шеринга
- [ ] Добавить брендированные изображения `opengraph-image.png` (динамические через `@vercel/og` или статические).
- [ ] Убедиться, что при шеринге страниц `/hire`, `/portfolio`, `/testimonials` в LinkedIn, X/Twitter и Telegram отображаются кликабельные карточки с правильным заголовком и описанием.

### 5. ⚡️ Code Quality & Consistency
- [ ] Провести аудит инлайн-стилей (`style={{ ... }}`), где используются жестко закодированные цвета фона и текста, чтобы все цвета безупречно следовали переменным `var(--...)` в светлой и тёмной темах.
